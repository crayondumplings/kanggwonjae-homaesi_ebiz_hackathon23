#############################
#         문제 5-1          #
#############################
#그림1
#7부터 1까지 2씩 차감 되는 별의 갯수 출력하기

num_star = 7 # 처음 별 갯수 지정 (7개)
# i가 num_star에서 0까지 2씩 차감 반복
for i in range(num_star, 0, -2): 
    
    #앞에 띄어 쓰기 할 ' ' 계산하기, 중앙에 정렬 되도록 띄어쓰기 값 만들기 
    #2씩 차감하니까 양쪽에 하나 하나씩 줘야 대칭이니까 나누기 2했을 때 값 만큼 ' ' 넣기
    spaces = ' ' * ((num_star - i) // 2)
    
    #줄 마다 필요한 별 갯수 정하기
    stars = '*' * i
    
    #위의 변수들 star_image라는 변수에 합해서 저장
    star_image = spaces + stars
    
    #결과 출력
    print(star_image)


#############################
#         문제 5-2          #
#############################
n = 4  # 별 모양 높이를 설정한다.

# i는 n에서 -n-1까지 1씩 감소한다.
# 이는 별 모양이 위에서 아래로 줄어들다가 다시 늘어나는 걸 보여준다.
for i in range(n, -n-1, -1):
    
    # 각 줄의 시작 부분에 공백을 추가한다.
    # 공백의 개수는 i의 절대값이다.
    # 이는 별 모양이 오른쪽으로 이동하다가 다시 왼쪽으로 이동하는 걸 보여준다.
    spaces = ' ' * abs(i)
    
    # 각 줄에 출력할 별의 개수를 설정한다.
    # 별의 개수는 2*(n-절대값(i))+1이다.
    # 이는 별 모양이 위에서 아래로 줄어들다가 다시 늘어나는 걸 보여준다.
    stars = '*' * (2*(n-abs(i))+1)
    
    # 각 줄의 끝 부분에 공백을 추가한다.
    # 공백의 개수는 i의 절대값이다.
    # 이는 별 모양이 왼쪽으로 이동하다가 다시 오른쪽으로 이동하는 걸 보여준다.
    end_spaces = ' ' * abs(i)
    
    # 공백, 별, 공백을 합쳐서 한 줄을 만든다.
    line = spaces + stars + end_spaces
    
    # 만들어진 줄을 출력한다.
    print(line)

------------------
#설명가능한 version
#그림2
#8,6,4,2,4,6,8 되는 모양 만들기

# 별 모양을 출력하는 함수 만들기
# 역삼각형 for문이랑 일반 삼각형 for문 합친 함수

def print_star(num_star):
    #역삼각형 8,6,4,2 부분
    # i가 num_star에서 0까지 2씩 차감 반복
    for i in range(num_star, 0, -2): 
        
        #앞에 띄어 쓰기 할 ' ' 계산하기, 중앙에 정렬 되도록 띄어쓰기 값 만들기 
        #2씩 차감하니까 양쪽에 하나 하나씩 줘야 대칭이니까 나누기 2했을 때 값 만큼 ' ' 넣기
        spaces = ' ' * ((num_star - i) // 2)
        
        #줄 마다 필요한 별 갯수 정하기
        stars = '*' * i
        
        #위의 변수들 star_image라는 변수에 합해서 저장
        star_image = spaces + stars
        
        #결과 출력
        print(star_image)
    #일반 삼각형 4,6,8 부분
    for j in range(4, num_star + 2, 2):
        
        #앞에 띄어 쓰기 할 ' ' 계산하기, 중앙에 정렬 되도록 띄어쓰기 값 만들기 
        #2씩 차감하니까 양쪽에 하나 하나씩 줘야 대칭이니까 나누기 2했을 때 값 만큼 ' ' 넣기
        
        spaces = ' ' * ((num_star - j) // 2) 
        #줄 마다 필요한 별 갯수 정하기
        
        stars = '*' * j  
        #위의 변수들 line이라는 변수에 합해서 저장
        
        star_image2 = spaces + stars  
        print(star_image2)

# 별 갯수 지정
num_star = 8

# 결과 출력
print_star(num_star)

#############################
#         문제 5-3          #
#############################
n = 5  # 별 모양 높이를 설정한다.

# i는 0에서 n-1까지 1씩 증가한다.
# 이는 별 모양이 위에서 아래로 그려지는 걸 보여준다.
for i in range(n):
    
    # 첫 번째 줄과 마지막 줄에는 별 10개를 그린다.
    if i == 0 or i == n-1:
        print('*' * 10)
    # 그 외의 줄에는 양 끝에 별 1개씩, 그 사이에 공백 8개를 그린다.
    else:
        print('*' + ' ' * 8 + '*')
